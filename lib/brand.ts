/**
 * The game's public name («en floke» is a tangle: sixteen words to untangle
 * into four threads). Used in the header, page titles and the shared
 * result text, so renaming the game is a one-line change.
 */
export const APP_NAME = "Floke";
